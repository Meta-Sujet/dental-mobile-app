import 'package:flutter/material.dart';

class MyCabinetPage extends StatelessWidget {
  const MyCabinetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
